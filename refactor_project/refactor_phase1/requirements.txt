# Core dependencies
pyzmq>=25.0.0
aiohttp>=3.9.0
asyncio-mqtt>=0.16.0
paho-mqtt>=1.6.0

# Optional GPS support (already in WarDragon)
# gpsd-py3>=0.3.0  # Uncomment if you need programmatic GPSD access
